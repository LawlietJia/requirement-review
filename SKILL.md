---
name: requirement-review
description: 对银行需求文档（需求书/软件需求说明书/需求申请单/PRD）做正式评审并产出结构化评审报告。当用户要求"评审/审查/体检"任何需求文档，要求检查需求质量、完整性、一致性、规则明确性，或要求核对"需求是否完整实现了业务方案/方案覆盖度"时，必须使用本 skill——即使用户只说"审一下这份软需""看看这个需求有没有问题""输出评审意见"。评审对象是本项目刚生成的需求文档时同样适用（写后自检）。不适用于：撰写新需求（用 /document 或 write-spec）、代码评审、文档润色、会议纪要、单纯版本对比/变更摘要（复审销号场景除外——那是本 skill 的跨轮复审功能）。
---

# 需求评审（requirement-review）

评审银行需求文档并产出**可机校、可销号、可复审**的评审报告。本 skill 的存在理由不是教你怎么发现问题（模型发现能力本身很强），而是**强制四类纪律结构**（源自基线实测失手点）：

1. **证据纪律**：无来源的精确数字是红旗不是需求；监管条文凭记忆一律待核验；合规红线定级不降级
2. **三字段定级**：影响级别与证据状态与放行效果分轴——"条件性 P1 待核验"不许冒充确认阻断
3. **review_status 先行**：信息不足就输出"待补充后重评"，不逼自己编三档结论
4. **标准化产物**：issues-ledger 固定 schema + 条目表落盘 + validate_review.py 硬校验

## 模式

- **完整评审**（默认）：S0-S9 十步全流程
- **快速体检**：全维度**浅扫**——每维只查**高后果类**检查项（数据正确性矛盾/合规红线/权限越权/依赖断链/无来源数字红旗），跳过质量与完善类检查（弱词措辞/界面惯例/术语统一/建议类——这些属 P2/P3 性质，留完整模式）。不跳过任何维度。
- **Gap 分析开关**（外购系统二开需求时用户指定开启）：见 references/dimensions.md §G
- 用户给 docx/pdf：提示先经 markitdown 转 md（本 skill 只收 md）

## 十步流程（S0-S9）

**S0 受理门禁**：确认文档路径与可选业务方案；确认模式与输出目录 `outputs/reviews/<需求名>-<日期>/`；做输入完整性检查（图片文字丢失/表格错位计数——涉图表类结论置信度受限，报告前置声明，转换噪声标 `[转换噪声]` 不计问题数）；预告成本量级（快速~几分钟；完整评审数十分钟、可能百万级 token）。

**S1 解析与理解确认**：读文档建立结构图；用 2-3 句复述需求核心（目标用户/解决什么/预期效果）**🔴 CHECKPOINT：请用户确认复述无误后再继续**（防方向错位——方向错了后面全白做）；**非交互模式**：无法等待确认时，把复述与理解假设记入报告开头（"评审基线假设"）后继续，S8 澄清问题里保留方向性确认项。超长文档（单上下文装不下）→ 提示降级为分章评审，**合并后必须做全局一致性检查**（矛盾/覆盖/依赖不因分章失效）。

**S2 需求条目原子化** → 落盘 `req-items.md`（复用文档已有编号；占位编号补 REQ-NNN 并标注）。

**S3 方案原子化**（仅有业务方案时）→ 按 references/traceability.md 的形态分级拆解，落盘 `biz-items.md`。

**S4 八维浅扫描**：按 references/dimensions.md 逐维过检查项，产八维×（完整/部分/缺失）仪表盘 → 决定 S5 深查优先级（薄弱维度全查、健康维度抽查）。

**S5 逐维深查**：每个发现记录进 issues-ledger（schema 见 references/report-template.md），**每条必附原文摘录**。视角镜头（业务/开发/测试/合规）融入检查作参考——无新增发现是正常结果，不为凑数造问题。

**S6 集合级审查**：同触发条件的需求两两比对（矛盾/多口径/时序）；依赖边从数据流信号/时序词/状态引用提取；全文关键词交叉检索同规则多口径（有效期/额度/时限/比例类词全查一遍）。

**S7 双向覆盖核对**（仅有业务方案时）：按 references/traceability.md 六步执行，落盘 `coverage-matrix.md`。

**S8 澄清循环**：交互场景——按影响×不确定性排序提问（一次一个，附候选选项，默认 5 问封顶，仅体验参数）；非交互——形成**条件性结论**，未答问题登记 Gap，**不得因缺答提升严重度**。

**S9 汇总定级与报告**：三字段定级（见下）→ review_status 先行 → 放行三问 → 报告（5 必出+**5 条件出**，模板见 references/report-template.md；条件出含**同业实践与优化建议**节——依据 L2/法规/L3 调研产出建议，标注来源，无依据则省略，建议不构成缺陷不影响放行）→ **🛑 STOP：跑 `python3 <skill目录>/scripts/validate_review.py <工作目录>`，零违规才算完成**（有违规必须修复后复跑，不得带违规交付报告）。

## 三字段定级（每条 Finding 独立三轴）

- **impact_level**（若问题成立的影响）：P0 灾难级/P1 严重/P2 一般/P3 建议——按业务后果×影响范围×修复时点评估
- **evidence_status**：已确认（文档内原文比对或一手来源）｜较高可信｜条件成立｜待核验
- **release_effect**：阻断｜条件放行（列明条件）｜待澄清｜不影响放行

**P0/P1 评价时点分界（防同型缺陷 P0/P1 摆动——V1.4 实测教训）**：评审对象是**尚未实施的设计**，绝大多数缺陷可通过修订需求关闭——这类一律 P1（含"按需求实施会违法"的口径错误，走 P1→阻断判据②）。**P0 仅限两类**：① 需求已确认要求的**行为本身违法/越红线**（行为性违法设计，如设计"拒收后仍同渠道再发"；区别于数字/期限/口径写错——后者修订即关）；② 追评/复审场景：错误已随上版进入生产或监管报送并造成不可逆外部化后果。首次评审的正常产出中 P0 应当罕见；把"评审时点发现、修订可关"的核心缺陷标为 P0，与漏报同级错误。

**铁律**：
- **阻断必须 evidence_status=已确认**（待核验/条件成立/较高可信 → 不得阻断，进待澄清或条件放行；校验器 fail-closed 强制）
- 🔴红线项（dimensions.md D7 标 🔴 的：数据出境无审批/最小必要违背/授权链缺失等**已确认违法**类）→ P0 不降级（基线曾把数据出境无审批定为"高"——错）；🟡一般合规缺陷（缺监管文号/引用泛化/制度类缺失）按影响定级，**缺文号≠已违法**
- 报告"必须修改项"只收 阻断项 + 条件放行的条件本身；待澄清进澄清清单

## 放行决策

**review_status**（已完成｜待补充）先定：关键信息不足以判断 → 输出"**待补充后重评**"+缺失清单，**不强行给三档结论**（禁止骑墙≠逼你编确定性）。
仅"已完成"时出三档，**分界规则（阻断语义绝对化——V1.6.3 起）**：任一 P0 或任一已确认阻断项 → **不通过**（阻断=不可带条件进入下一阶段；校验器机械强制：存在阻断而结论非"不通过"即违规）；**可带条件关闭的缺陷应标 `条件放行`（列明条件与复核判据）而非 `阻断`**——"阻断"只留给必须修订后重审的实质破坏（P1→阻断三判据命中）。零阻断但有未消条件放行条件 → **有条件通过**；否则**通过**。
**P1→阻断升级判据**（已确认 P1 满足任一即阻断，否则条件放行列明条件）：
① 破坏核心业务链路**数据正确性**（底层数据被掏空/计算链路断裂/口径错致结果系统性错误）
② 监管报送或合规口径实质错误（🔴红线类，见 D7 分级）
③ 破坏审计追溯完整性（已生效数据不可追溯）。
**判据适用下限（防系统性高判——V1.1 实测教训）**：判据针对**数据正确性/合规/审计的实质破坏**，**不适用于表述质量类缺陷**——原子性不足、弱词、可测性差、术语不统一、编号缺失属于质量问题，即使影响理解也不构成"破坏链路"，其 impact 上限通常为 P2/P3；把 P3 级质量问题升级为 P0/P1 阻断与漏报同级错误。不确定是否"实质破坏"时→条件放行+澄清，不定阻断。
放行三问：①有无已确认阻断项？②有无妨碍评审判断的重要未知？③文档当前是否具备决策所需信息？

## 证据纪律（贯穿全程）

1. **无来源精确数字红旗**：凭空阈值/上限（"最多 500 条"）→ 要求出处，无出处按未证实陈述处理进澄清清单（基线曾把 500 当真实需求用了）
2. **监管引用**：以 references/regulations.md 为准（已一手核验）；表外法规触发 L3 调研核验官方原文后才能定级引用；**agent/记忆带回的文号一律待核验**
3. **外部事实**：URL+来源类型+核验日期三字段齐备才能支撑 P0/P1
4. **薄文档比例原则**：短文档裁剪 P2/P3 深度，但伪造证据/关键歧义/必须覆盖缺口/合规红线任何规模不豁免；也不为显得严格而制造问题

## 知识调研（按 references/research-protocol.md 执行）

三级：L1 项目内（inputs/docs/ 历史需求书密度最高）→ L2 Obsidian 知识库（grep 探测，相关+权威+适用才深查）→ L3 外部（仅 7 类触发；**数据安全红线：只发去标识化抽象问题；🔴 CHECKPOINT：首次外发前去标识化查询文本在会话中明示并等用户确认后才发出；严禁外发需求原文/行内系统名/客户数据**）。L3 按**能力探测**路由：环境中可用的 deep-research/Workflow 编排能力优先，其次任一可用搜索工具（按全局搜索优先级链），均不可用或问题无法安全抽象 → 降级 Gap 登记并明示，**严禁凭常识补结论**。全程记 `research-log.md`。

## 报告降噪分层（对作者友好）

问题清单分两层呈现：
- **PRD 必须决定**（进"必须修改项/澄清"）：业务规则、状态流转、角色权限、范围口径、验收标准——需求阶段不解决会传导向后
- **RFC/设计阶段跟进**（单独一节，不阻塞放行）：存储方式、队列/重试、日志实现、接口技术细节——PRD 只要不与之矛盾即可
"必须修改项"开头先列**最关键的 3-5 项**（作者优先处理），其余按 F-ID 完整列出。

## 重要文档双跑规则（应对 run 间方差）

盲测实证：同一 skill 双 run 的漏检集合不同（并集召回高于任一单 run，但任何两 run 并集也未必全覆盖），且个别定级在 P0/P1/P2 间摆动。**双跑决策规则**：上会/交付级文档 → **必须双跑**（按下述流程）；日常评审 → 单跑 + 人工过一遍阻断项判据引用（约 30 秒/条）。双跑流程：

- 两次完整评审（新上下文）后，**候选 Finding 取并集**
- 并集后**逐条证据归并与去重**（同一缺陷两 run 表述不同 → 合并，保留更完整的证据链与原文锚点）
- **严重度与放行效果不得机械取并集**——对并集后的每条重新裁决定级（单 run 的 P0 高判不得因并集被放大；以 P0/P1 评价时点分界为裁决尺度）
- **双跑后人工重点复核机械比对三项**：D3-4 术语漂移 / D3-10 码值逐值 / D3-11 字段链——盲测实证此三类检查单 run 执行不稳定（术语漂移型 10 run 仅 3 次发现），肉眼难辨，须人工 grep 复核
- 阻断集合以裁决后的合并台账为准，最终报告基于合并台账生成

## 跨轮复审

同文档再评审：读上轮 issues-ledger → 两版语义 diff（原文锚定，防编号漂移）→ 只深查 上轮问题销号情况 + 改动需求 + 依赖闭包涟漪 → 报告加"上轮问题销号状态"列。F-ID 延续编号不复用。

## References 索引（按需读，勿全量预载）

| 文件 | 何时读 |
|---|---|
| references/dimensions.md | S4/S5 全评审必读（四层八维检查项，⚠️ 标基线失手点） |
| references/traceability.md | S3/S7 有业务方案时必读 |
| references/research-protocol.md | 触发调研时读（含数据安全红线全文） |
| references/regulations.md | D7 合规维度必读；引用任何法规前对照（含适用范围与状态动态判定） |
| references/sources.md | 检查项来源存疑时查（Source ID 注册表+验证状态） |
| references/report-template.md | S9 报告生成必读（ledger schema + 报告模板） |
